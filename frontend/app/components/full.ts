import { styled } from "@mui/system";


const Full = styled("div")({
    width: '100%',
    height: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
});

export {Full}
